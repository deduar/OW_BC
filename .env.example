POSTGRES_USER=owbc
POSTGRES_PASSWORD=owbc_pass
POSTGRES_DB=owbc
DB_HOST=db
DB_PORT=5432

AUTH_SECRET_KEY=change-me

# Frontend origin allowed by CORS (for local dev)
CORS_ORIGIN=http://localhost:3000

# Upload limits (bytes)
MAX_UPLOAD_SIZE_BYTES=10485760

# Dev image tag/version used by docker-compose
OWBC_VERSION=0.1.0-dev

